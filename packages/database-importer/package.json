{
  "name": "@rapidroute/database-importer",
  "scripts": {
    "build": "tsc",
    "start": "(npx tsc || exit 0) && node dist/src/index.js",
    "lint": "npx eslint . --fix",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": "16"
  },
  "main": "dist/index.js",
  "dependencies": {
    "@rapidroute/database-types": "^1.0.0",
    "@types/node-fetch": "^2.6.2",
    "firebase-admin": "^11.0.1"
  },
  "devDependencies": {
    "typescript": "^4.8.3"
  }
}
